# Subscription Tier CI PR Validation

Purpose: validate the `pull_request` advisory workflow path.

Workflow: `Subscription Tier Proof Gate`.

Expected behavior: the check runs on this docs-only pull request, but it does not block because the workflow remains advisory for PR events.

No product, app, runtime, subscription, Stripe, checkout, entitlement, package, lockfile, schema, migration, workflow, branch protection, or app-internal behavior changed.
